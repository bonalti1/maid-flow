# ── Secrets — never commit real API keys ──
.env
.env.*
*.local
# keep templates / examples (no real values)
!.env.example
!.env.*.example

# ── Dependencies & build output ──
node_modules/
dist/
build/

# ── Local tooling / editor ──
.claude/

# ── OS / editor noise ──
.DS_Store
*.log

# ── Runtime local DB (json store) ──
tradetechpro/server/data/
